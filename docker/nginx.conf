server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    add_header Permissions-Policy "local-network=(self), loopback-network=(self), local-network-access=(self)" always;

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_buffering off;          # SSE 任务进度
        proxy_read_timeout 3600s;
    }
    location /uploads/ { proxy_pass http://backend:8000; }
    location /outputs/ { proxy_pass http://backend:8000; }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
